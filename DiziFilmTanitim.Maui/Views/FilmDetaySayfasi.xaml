<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DiziFilmTanitim.MAUI.Views.FilmDetaySayfasi"
             BackgroundColor="{StaticResource Background}">

    <Grid>
        <!-- Background -->
        <Rectangle Fill="{StaticResource PrimaryGradient}"
                   Opacity="0.05"/>

        <ScrollView Padding="20">
            <VerticalStackLayout Spacing="25">

                <!-- Loading -->
                <VerticalStackLayout IsVisible="{Binding IsBusy}"
                                     VerticalOptions="Center"
                                     Spacing="20">
                    <ActivityIndicator IsRunning="{Binding IsBusy}"
                                       Color="{StaticResource Primary}"
                                       WidthRequest="40"
                                       HeightRequest="40"/>
                    <Label Text="Film detayları yükleniyor..."
                           FontSize="16"
                           TextColor="{StaticResource TextSecondary}"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>

                <!-- Film Detay Content -->
                <VerticalStackLayout IsVisible="{Binding VeriYuklendi}"
                                     Spacing="25">

                    <!-- Film Header -->
                    <Border Style="{StaticResource ModernCard}">
                        <Grid ColumnDefinitions="120,*"
                              ColumnSpacing="20"
                              Padding="10">

                            <!-- Film Afişi -->
                            <Border Grid.Column="0"
                                    BackgroundColor="{StaticResource PrimaryLight}"
                                    StrokeThickness="0"
                                    StrokeShape="RoundRectangle 10"
                                    WidthRequest="110"
                                    HeightRequest="160">
                                <Image Aspect="AspectFill"
                                       Source="{Binding Film.AfisUrl}"/>
                            </Border>

                            <!-- Film Bilgileri -->
                            <VerticalStackLayout Grid.Column="1"
                                                 Spacing="10"
                                                 VerticalOptions="Center">

                                <!-- Film Adı -->
                                <Label Text="{Binding Film.Ad}"
                                       FontSize="22"
                                       FontAttributes="Bold"
                                       TextColor="{StaticResource Primary}"
                                       LineBreakMode="WordWrap"/>

                                <!-- Yıl ve Süre -->
                                <HorizontalStackLayout Spacing="20">
                                    <HorizontalStackLayout Spacing="5">
                                        <Label Text="📅"
                                               FontSize="14"/>
                                        <Label Text="{Binding Film.YapimYiliText}"
                                               FontSize="16"
                                               FontAttributes="Bold"
                                               TextColor="{StaticResource TextPrimary}"/>
                                    </HorizontalStackLayout>
                                    <HorizontalStackLayout Spacing="5">
                                        <Label Text="⏱️"
                                               FontSize="14"/>
                                        <Label Text="{Binding Film.SureText}"
                                               FontSize="16"
                                               TextColor="{StaticResource TextSecondary}"/>
                                    </HorizontalStackLayout>
                                </HorizontalStackLayout>

                                <!-- Yönetmen -->
                                <HorizontalStackLayout Spacing="5">
                                    <Label Text="🎭"
                                           FontSize="14"/>
                                    <Label Text="{Binding Film.YonetmenAdi}"
                                           FontSize="16"
                                           TextColor="{StaticResource TextPrimary}"
                                           LineBreakMode="WordWrap"/>
                                </HorizontalStackLayout>

                            </VerticalStackLayout>

                        </Grid>
                    </Border>

                    <!-- Türler -->
                    <Border Style="{StaticResource ModernCard}">
                        <VerticalStackLayout Spacing="10">
                            <Label Text="🏷️ Türler"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource Primary}"/>
                            <Label Text="{Binding Film.TurlerText}"
                                   FontSize="16"
                                   TextColor="{StaticResource TextPrimary}"
                                   LineBreakMode="WordWrap"/>
                        </VerticalStackLayout>
                    </Border>

                    <!-- Oyuncular -->
                    <Border Style="{StaticResource ModernCard}">
                        <VerticalStackLayout Spacing="10">
                            <Label Text="🎭 Oyuncular"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource Primary}"/>
                            <Label Text="{Binding Film.OyuncularText}"
                                   FontSize="16"
                                   TextColor="{StaticResource TextPrimary}"
                                   LineBreakMode="WordWrap"/>
                        </VerticalStackLayout>
                    </Border>

                    <!-- Özet -->
                    <Border Style="{StaticResource ModernCard}"
                            IsVisible="{Binding Film.OzetVarMi}">
                        <VerticalStackLayout Spacing="10">
                            <Label Text="📝 Özet"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource Primary}"/>
                            <Label Text="{Binding Film.Ozet}"
                                   FontSize="16"
                                   TextColor="{StaticResource TextPrimary}"
                                   LineBreakMode="WordWrap"/>
                        </VerticalStackLayout>
                    </Border>

                    <!-- Puanlama Sistemi -->
                    <Border Style="{StaticResource ModernCard}">
                        <VerticalStackLayout Spacing="15">

                            <Label Text="★ Puanlama"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource Primary}"/>

                            <!-- Ortalama Puan -->
                            <HorizontalStackLayout Spacing="10">
                                <Label Text="Film Puanı:"
                                       FontSize="16"
                                       TextColor="{StaticResource TextSecondary}"
                                       VerticalOptions="Center"/>
                                <Label Text="{Binding OrtalamaPuanText}"
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       TextColor="{StaticResource Primary}"
                                       VerticalOptions="Center"/>
                            </HorizontalStackLayout>

                            <!-- Kullanıcı Puanı (eğer varsa) -->
                            <HorizontalStackLayout Spacing="10"
                                                   IsVisible="{Binding KullaniciGirisYapti}">
                                <Label Text="{Binding KullaniciPuaniText}"
                                       FontSize="14"
                                       TextColor="{StaticResource Secondary}"
                                       VerticalOptions="Center"/>
                            </HorizontalStackLayout>

                            <!-- Puanlama Yıldızları -->
                            <VerticalStackLayout Spacing="10">
                                <Label Text="Puan Verin:"
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       TextColor="{StaticResource TextPrimary}"
                                       IsVisible="{Binding KullaniciGirisYapti}"/>

                                <HorizontalStackLayout Spacing="8"
                                                       HorizontalOptions="Center"
                                                       IsVisible="{Binding KullaniciGirisYapti}">
                                    <Button Text="★"
                                            CommandParameter="1"
                                            Command="{Binding PuanVerCommand}"
                                            Style="{StaticResource StarButton}"/>
                                    <Button Text="★"
                                            CommandParameter="2"
                                            Command="{Binding PuanVerCommand}"
                                            Style="{StaticResource StarButton}"/>
                                    <Button Text="★"
                                            CommandParameter="3"
                                            Command="{Binding PuanVerCommand}"
                                            Style="{StaticResource StarButton}"/>
                                    <Button Text="★"
                                            CommandParameter="4"
                                            Command="{Binding PuanVerCommand}"
                                            Style="{StaticResource StarButton}"/>
                                    <Button Text="★"
                                            CommandParameter="5"
                                            Command="{Binding PuanVerCommand}"
                                            Style="{StaticResource StarButton}"/>
                                </HorizontalStackLayout>

                                <!-- Giriş Yapmamış Kullanıcı Mesajı -->
                                <VerticalStackLayout Spacing="10"
                                                     IsVisible="{Binding KullaniciGirisYapmadi}">
                                    <Label Text="Puan vermek için giriş yapmalısınız."
                                           FontSize="14"
                                           TextColor="{StaticResource TextSecondary}"
                                           HorizontalOptions="Center"/>
                                    <Button Text="Giriş Yap"
                                            Command="{Binding GirisYapCommand}"
                                            Style="{StaticResource PrimaryButton}"
                                            HorizontalOptions="Center"
                                            WidthRequest="120"/>
                                </VerticalStackLayout>
                            </VerticalStackLayout>

                        </VerticalStackLayout>
                    </Border>

                    <!-- İster 16: İzleme Planlama (DatePicker & TimePicker) -->
                    <Border Style="{StaticResource ModernCard}">
                        <VerticalStackLayout Spacing="15">
                            <Label Text="📅 İzleme Planlama"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource Primary}"/>

                            <VerticalStackLayout Spacing="15"
                                                 IsVisible="{Binding KullaniciGirisYapti}">

                                <!-- İzleme Tarihi -->
                                <VerticalStackLayout Spacing="8">
                                    <Label Text="İzleme Tarihi:"
                                           FontSize="16"
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource TextPrimary}"/>
                                    <Border Style="{StaticResource ModernEntry}">
                                        <DatePicker Date="{Binding IzlemeTarihi}"
                                                    FontSize="16"
                                                    TextColor="{StaticResource TextPrimary}"
                                                    BackgroundColor="Transparent"
                                                    Format="dd/MM/yyyy"/>
                                    </Border>
                                </VerticalStackLayout>

                                <!-- İzleme Saati -->
                                <VerticalStackLayout Spacing="8">
                                    <Label Text="İzleme Saati:"
                                           FontSize="16"
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource TextPrimary}"/>
                                    <Border Style="{StaticResource ModernEntry}">
                                        <TimePicker Time="{Binding IzlemeSaati}"
                                                    FontSize="16"
                                                    TextColor="{StaticResource TextPrimary}"
                                                    BackgroundColor="Transparent"
                                                    Format="HH:mm"/>
                                    </Border>
                                </VerticalStackLayout>

                                <!-- Hatırlatma -->
                                <HorizontalStackLayout Spacing="10">
                                    <CheckBox IsChecked="{Binding HatirlatmaAktif}"
                                              Color="{StaticResource Primary}"
                                              VerticalOptions="Center"/>
                                    <Label Text="Hatırlatma gönder"
                                           FontSize="16"
                                           TextColor="{StaticResource TextPrimary}"
                                           VerticalOptions="Center"/>
                                </HorizontalStackLayout>

                                <!-- Planlama Butonu -->
                                <Button Text="📝 İzleme Planını Kaydet"
                                        Command="{Binding IzlemePlaniKaydetCommand}"
                                        Style="{StaticResource SecondaryButton}"
                                        HorizontalOptions="Fill"/>

                                <!-- Mevcut Plan (varsa) -->
                                <Border BackgroundColor="{StaticResource SecondaryLight}"
                                        StrokeThickness="0"
                                        StrokeShape="RoundRectangle 8"
                                        Padding="15"
                                        IsVisible="{Binding IzlemePlaniVarMi}">
                                    <VerticalStackLayout Spacing="5">
                                        <Label Text="📋 Mevcut Planınız:"
                                               FontSize="14"
                                               FontAttributes="Bold"
                                               TextColor="{StaticResource Secondary}"/>
                                        <Label Text="{Binding IzlemePlaniMetni}"
                                               FontSize="14"
                                               TextColor="{StaticResource TextPrimary}"/>
                                    </VerticalStackLayout>
                                </Border>
                            </VerticalStackLayout>

                            <!-- Giriş Yapmamış Kullanıcı -->
                            <VerticalStackLayout Spacing="10"
                                                 IsVisible="{Binding KullaniciGirisYapmadi}">
                                <Label Text="İzleme planı oluşturmak için giriş yapmalısınız."
                                       FontSize="14"
                                       TextColor="{StaticResource TextSecondary}"
                                       HorizontalOptions="Center"
                                       HorizontalTextAlignment="Center"/>
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                    </Border>

                    <!-- Listeye Ekle -->
                    <Border Style="{StaticResource ModernCard}">
                        <VerticalStackLayout Spacing="15">
                            <Label Text="📝 Listem"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource Primary}"/>

                            <!-- Giriş Yapmış Kullanıcı -->
                            <VerticalStackLayout Spacing="10"
                                                 IsVisible="{Binding KullaniciGirisYapti}">
                                <Button Text="+ Listeye Ekle"
                                        Command="{Binding ListeyeEkleCommand}"
                                        Style="{StaticResource SecondaryButton}"
                                        HorizontalOptions="Fill"/>
                            </VerticalStackLayout>

                            <!-- Giriş Yapmamış Kullanıcı -->
                            <VerticalStackLayout Spacing="10"
                                                 IsVisible="{Binding KullaniciGirisYapmadi}">
                                <Label Text="Film listelerinize eklemek için giriş yapmalısınız."
                                       FontSize="14"
                                       TextColor="{StaticResource TextSecondary}"
                                       HorizontalOptions="Center"
                                       HorizontalTextAlignment="Center"/>
                                <Button Text="Giriş Yap"
                                        Command="{Binding GirisYapCommand}"
                                        Style="{StaticResource PrimaryButton}"
                                        HorizontalOptions="Center"
                                        WidthRequest="120"/>
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                    </Border>

                    <!-- Geri Butonu -->
                    <Button Text="← Geri Dön"
                            Command="{Binding GeriGitCommand}"
                            Style="{StaticResource SecondaryButton}"
                            HorizontalOptions="Center"
                            WidthRequest="150"/>

                </VerticalStackLayout>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>